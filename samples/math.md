# Math Sample

Inline math should be detected inside normal text, such as $e^{i\pi} + 1 = 0$.

Display math should render as its own preview block:

$$
a^2 + b^2 = c^2
$$

Escaped dollars should stay as regular text: \$5.
