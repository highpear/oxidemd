# Inline Sample

This paragraph includes **strong text**, *emphasis*, and `inline code`.

You can also mix **bold text with `code` inside the same line** for quick checks.

Visit [Rust](https://www.rust-lang.org/) to confirm link styling.

> Quotes can include **strong text** and *emphasis* too.

- A list item with **bold text**
- A list item with `inline code`
- A list item with a [link](https://example.com)
